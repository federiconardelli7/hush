// The fixed reaction set. ❤️ is the default (the one-tap like); the rest are pick-one
// alternates offered in the thread's reaction picker. One reaction per person.
export const REACTION_EMOJIS = ["❤️", "😂", "🎉", "👍", "😮", "🔥"] as const;

export const DEFAULT_REACTION = "❤️";
